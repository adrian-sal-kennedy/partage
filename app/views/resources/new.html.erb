<%= render 'shared/navbar' %>
<div class="home-column">
  <div class="home-column-panel">
    <h1>Add a new resource for hire.</h1>
    <h2><%= "#{@business.name}" %></h2>
    <%= render partial: 'resource_form', locals: {business: @business, resource: @resource, user: @user} %>
  </div>
</div>